<% if fav_book.favorited_by?(current_user) %>
  <p>
    <%= link_to book_favorites_path(fav_book), method: :delete, remote: true, data: {"turbolinks" => false} do %>
      <i class="fas fa-heart" aria-hidden="true" style="color: red;"></i>
      <span style="color: red;"><%= fav_book.favorites.count %></span>
    <% end %>
  </p>
  <% else %>
  <p>
      <%= link_to book_favorites_path(fav_book), method: :post, remote: true, data: {"turbolinks" => false} do %>
        ♡<%= fav_book.favorites.count %>
      <% end %>
  </p>
<% end %>